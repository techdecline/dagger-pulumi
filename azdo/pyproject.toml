[project]
name = "azdo"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["dagger-io","azure-devops","msrest"]

[tool.uv.sources]
dagger-io = { path = "sdk", editable = true }

[build-system]
requires = ["hatchling==1.25.0"]
build-backend = "hatchling.build"
